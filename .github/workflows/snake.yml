name: generate animation

on:
  schedule:
    - cron: "0 */24 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: cryptocoderr
          outputs: dist/github-contribution-grid-snake.svg
